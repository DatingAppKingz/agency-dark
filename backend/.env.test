# Test environment configuration
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/agencydark_test
REDIS_URL=redis://localhost:6379/1
SECRET_KEY=test_secret_key_for_testing_only_not_for_production
FRONTEND_URL=http://localhost:3000
ENVIRONMENT=test
DEBUG=True

# API Keys for testing
INFLOW_API_KEY=test_inflow_key
ONLYFANS_API_KEY=test_onlyfans_key

# Email settings (disabled for tests)
SMTP_HOST=
SMTP_PORT=
SMTP_USER=
SMTP_PASSWORD=
SMTP_FROM=

# Upload settings
UPLOAD_DIR=test_uploads
MAX_UPLOAD_SIZE=5242880

# Rate limiting (relaxed for tests)
RATE_LIMIT_CALLS=1000
RATE_LIMIT_PERIOD=60